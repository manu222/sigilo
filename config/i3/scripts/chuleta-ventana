#!/usr/bin/env bash
# Abre la chuleta en una ventana flotante. i3 la centra por su titulo.
exec "$HOME/.config/i3/scripts/terminal" --titulo chuleta-i3 --tam 152x46 \
     -- bash -c "$HOME/.config/i3/scripts/chuleta; read -n1 -s"
